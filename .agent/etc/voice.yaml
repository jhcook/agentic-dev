llm:
  provider: gemini
  model: gemini-2.0-flash
stt:
  provider: google
  model: default
tts:
  provider: google
  model: en-US-Neural2-F
# stt:
#   provider: deepgram_streaming
#   model: nova-2
# tts:
#   provider: deepgram
#   model: aura-orion-en  # Australian English accent
whisper:
  model_size: tiny
  device: auto

# Audio and VAD Tuning
vad:
  aggressiveness: 0.5    # 0 (Least aggressive) to 3 (Most aggressive)
  rms_threshold: 300     # Minimum volume level (RMS) to process audio as speech
  silence_threshold: 0.7 # Seconds of silence to trigger response
