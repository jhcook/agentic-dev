id: JRN-002
title: Workflow Token Optimization
state: COMMITTED
schema_version: 1
priority: medium
tags:
- infra
- cli
actor: developer using agent CLI
description: '### Observability

  - [x] Logs are structured and free of PII'
preconditions:
- Agent CLI is installed and configured
- Developer is in a project directory with .agent/ initialized
auth_context:
  level: authenticated
  permissions: []
steps:
- id: 1
  action: Developer runs `agent commit`
  system_response: CLI processes the commit request
  assertions:
  - Command exits with status 0
  - Expected output displayed
- id: 2
  action: Developer runs `agent commit --help`
  system_response: CLI processes the --help request
  assertions:
  - Command exits with status 0
  - Expected output displayed
- id: 3
  action: Developer runs `agent commit -m`
  system_response: CLI processes the -m request
  assertions:
  - Command exits with status 0
  - Expected output displayed
- id: 4
  action: Developer runs `agent new-runbook`
  system_response: CLI processes the new-runbook request
  assertions:
  - Command exits with status 0
  - Expected output displayed
- id: 5
  action: Developer runs `agent new-runbook --help`
  system_response: CLI processes the --help request
  assertions:
  - Command exits with status 0
  - Expected output displayed
error_paths:
- trigger_step: 1
  condition: Missing configuration or prerequisites
  system_response: CLI displays clear error with remediation steps
  assertions:
  - Error message is actionable
  severity: expected
edge_cases:
- scenario: Command run with no network access
  expected: Graceful degradation or clear offline error
  severity: warning
data_state:
  data_classification: internal
  inputs: []
  persistent: []
linked_stories:
- INFRA-002
linked_adrs: []
depends_on: []
implementation:
  routes: []
  files:
  - GEMINI.md
  - agents.yaml
  - config.py
  - router.yaml
  - task.md
  tests:
  - .agent/tests/journeys/test_jrn_002.py
