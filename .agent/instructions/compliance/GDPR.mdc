# GDPR Compliance Checklist

## Data Minimization
- [ ] We only collect data that is strictly necessary.
- [ ] PII is not logged to console or text files.

## Right to Erasure
- [ ] There is a mechanism to delete this user's data.
- [ ] Backups are handled (or at least documented) regarding deletion.

## Consent
- [ ] Users explicitly consent to data collection.
- [ ] Cookie banners are present if tracking is used.

## Security
- [ ] Data at rest is encrypted.
- [ ] Data in transit is encrypted (TLS).

## 📝 Output Formatting

When generating your findings and listing necessary modifications in the `REQUIRED_CHANGES` section, you **MUST** include a `(Source: <filepath or ADR>)` citation for each item to prove the change is grounded in actual codebase context and policy.

Example:
```
REQUIRED_CHANGES:
- change 1 (Source: path/to/file.py): Details of the change.
```
